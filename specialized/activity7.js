let  numStrokes

numStrokes = window. prompt("25") 

// Assumes "par 4"  if numStrokes < = 0
console. log ("invalid entry.")
if (numStrokes == 1){
}  console. log ("hole in 1!!!")
if (numStrokes == 2){
}  console. log ("Eagle!")
if (numStrokes == 3){
}  console. log ("Birdie.")
if (numStrokes == 4){
}console. log ("Par.")
   {console. log ("Better luck next time.")
}